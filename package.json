{
  "name": "hosts",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "update-hosts": "node scripts/filter_hosts.js"
  },
  "engines": {
    "node": ">=24.2.0"
  }
}
