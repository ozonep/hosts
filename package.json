{
  "name": "hosts",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/filter_hosts.test.js",
    "update-hosts": "node scripts/filter_hosts.js"
  },
  "engines": {
    "node": ">=22"
  }
}
